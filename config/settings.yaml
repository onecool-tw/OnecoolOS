app:
  name: Onecool OS
  version: 0.2.0
  timezone: Asia/Taipei
  language: en

database:
  path: data/onecool_os.sqlite3

paths:
  data_dir: data
  cache_dir: data/cache
  logs_dir: data/logs
  exports_dir: data/exports

runtime:
  debug: false
  environment: production

# Example secret placeholders for local deployments.
#
# Copy this file to a local secrets file when needed. Do not commit real
# credentials, tokens, API keys, or passwords.

secrets:
  github_token: ""
  database_password: ""

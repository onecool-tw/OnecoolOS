# Local user overrides for Onecool OS.
#
# This file is safe to edit for local preferences. Do not store credentials or
# secrets here; copy config/secrets.example.yaml to an ignored local secrets file
# when secret placeholders are needed.

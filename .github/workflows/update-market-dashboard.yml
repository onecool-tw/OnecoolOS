name: Update Market Dashboard

on:
  workflow_dispatch:
    inputs:
      refresh_actions_group:
        description: Refresh one API-safe dashboard dividend/split group
        required: false
        type: choice
        options:
          - none
          - group_a
          - group_b
        default: none
  schedule:
    # US close: 05:00 Asia/Taipei, Tuesday-Saturday.
    - cron: "0 21 * * 1-5"
    # Taiwan/Japan/Korea close: 16:00 Asia/Taipei, Monday-Friday.
    - cron: "0 8 * * 1-5"

permissions:
  contents: write

concurrency:
  group: update-market-dashboard
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install project
        run: pip install -e .
      - name: Update Market Dashboard cache
        env:
          ALPHA_VANTAGE_API_KEY: ${{ secrets.ALPHA_VANTAGE_API_KEY }}
        run: |
          args=()
          # Prices refresh every run. Alpha Vantage corporate-action checks
          # only run on the 21:00 UTC US-close schedule to avoid duplicate calls.
          if [[ "${{ inputs.refresh_actions_group }}" != "" && "${{ inputs.refresh_actions_group }}" != "none" ]]; then
            args+=(--refresh-actions-group "${{ inputs.refresh_actions_group }}")
          elif [[ "${{ github.event_name }}" == "schedule" && "$(date -u +%H)" == "21" && "$(date -u +%u)" == "1" ]]; then
            args+=(--refresh-actions-group group_a)
          elif [[ "${{ github.event_name }}" == "schedule" && "$(date -u +%H)" == "21" && "$(date -u +%u)" == "2" ]]; then
            args+=(--refresh-actions-group group_b)
          fi
          python scripts/update_market_dashboard.py "${args[@]}"
      - name: Commit only a complete dashboard update
        run: |
          git add data/market/dashboard
          if git diff --cached --quiet; then
            echo "No new Market Dashboard data."
            exit 0
          fi
          git config user.name "onecool-os-bot"
          git config user.email "actions@users.noreply.github.com"
          git commit -m "Update Market Dashboard"
          git push

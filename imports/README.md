# Imports

This directory stores raw files exported from external platforms before they
are normalized into Onecool OS internal data.

Raw imports may include PSA Collection CSV files, eBay Orders CSV files, Card
Ladder exports, COMC exports, and BGS exports.

Raw exported files are personal data and are ignored by Git. Keep README files
in place so the import directory structure remains visible in the repository.

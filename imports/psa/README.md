# PSA Imports

Place PSA Collection CSV exports in this directory.

Example workflow:

1. Export PSA Collection CSV from PSA.
2. Save the file under `imports/psa/`.
3. Run `python -m onecool_os cards import-csv imports/psa/<file>.csv`.
